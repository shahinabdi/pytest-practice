import pytest
from src.converter import CurrencyConverter


@pytest.fixture(scope="function")
def converter():
    """TODO 1: Create a fixture that provides a fresh CurrencyConverter instance

    Requirements:
    - Should return a new CurrencyConverter instance for each test
    - Should use function scope
    - Should have proper docstring
    """
    pass
